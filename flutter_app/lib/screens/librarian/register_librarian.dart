import 'package:flutter/material.dart';

class RegistrationLibrarianPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Librarian Register'),
      ),
      body: Center(
        child: Text(
          'Librarian Register Page',
          style: TextStyle(fontSize: 24.0),
        ),
      ),
    );
  }
}
